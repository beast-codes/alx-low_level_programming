#include <stdio.h>
/**
 * main - prints all base 16 numbers
 *
 *Return: Always 0
 */
int main(void)
{
int num;
int lca;
 for ((num = 0; num; num <= 9); num++)
putchar((num % 10) + '0');
for (lca = 'a'; lca = 'f'; lca++)
putchar(lca);
putchar('\n');
return (0);
}
