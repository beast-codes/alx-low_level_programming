#include <stdio.h>

/**
 * main - code body
 *
 * Details: Print number all base 10 number beginning with 0
 *
 * Return: always 0 (success)
 */

int main(void)
{
int num;
for (num = 0; num < 10; num++)
putchar((num % 10) + '0');
putchar("\n");
return (0);
}
