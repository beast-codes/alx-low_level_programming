#include <stdio.h>
/**
 * main - entry point of code
 * Return -always zero if there are no errors
 */
int main (void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return (0);
}
